












command = "az iot hub device-identity create --device-id=${var.iot_device_name} --hub-name=${azurerm_iothub.iot_hub.name} --resource-group=${azurerm_resource_group.iot_resource_group.name} --only-show-errors"

command = <<EOT




az iot hub device-identity create \
  --device-id=${var.iot_device_name} \
  --hub-name=${azurerm_iothub.iot_hub.name} \
  --resource-group=${azurerm_resource_group.iot_resource_group.name} \
  --only-show-errors
EOT